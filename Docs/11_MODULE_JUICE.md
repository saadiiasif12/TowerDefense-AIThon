# 11 · Module: Juice — VFX/SFX/Haptics/Audio (RoyalSiege.Juice / RoyalSiege.Audio)

**Status: VFX CORE LIVE — 17 Jul.** Implemented: `VfxSpawner` (pooled one-shots, tint/scale per spawn), `JuiceDirector` (EnemyKilled→puff, BuildingPlaced→dust, SpellCast→tinted burst per spell), `ZapArcRenderer` (Tesla jagged additive arcs + sparks via InstantShotFired), `BuildingTurret` (yaw tracking, anticipation→recoil kick, Tesla charge-squash/pop, ease-out-back placement pop-in), projectile juice (per-settings muzzle/impact VFX, impact tint, spin; metallic cannonball + Meshy Arrow bolt + purple emissive mage bolt, all with trails). Trajectory heights raised for tower clearance. Remaining for Day 3: SFX, haptics, camera shake, hit-stop, boss-slam telegraph decal, freeze tint on enemies, music.
Tuning knobs: BuildingTurret serialized fields per prefab; ProjectileSettingsSO juice block; `_yawModelOffsetDegrees` if a mesh's barrel axis isn't +Z.

**17 Jul additions:** X-Bow nocked-arrow cycle — `BuildingTurret._loadedAmmo` (the Arrow model on the bow): hidden the instant the shot fires ("becomes" the flying bolt), pops back with a 0.12 s ease-out-back on the next windup. X-Bow mesh fires along local −X → `_yawModelOffsetDegrees = 90` on its prefab (measured live: arrow long-axis vs firing line delta 0°). Muzzle moved to the nocked-arrow position so the projectile continues from it. If Cannon's barrel ever looks misaligned, apply the same live measurement (arrow/barrel world bounds vs pivot yaw) and set its offset.

## Juice map (event → response)

| Event | VFX | SFX | Haptic |
|---|---|---|---|
| Enemy killed | death puff + orb spawn | pop | light tick |
| Orb credited | bar flash +count | coin chime | — |
| Card drag start | ghost + range ring | pick | selection tick |
| Building placed | dust slam + ring pulse | thud | medium |
| Spell Arrows | arrow rain | whoosh-thunk | light |
| Spell Fireball | explosion + camera shake (small) | boom | medium |
| Spell Freeze | frost burst + tinted enemies | crystal | light |
| Spell Lightning | 3 bolts + flash + hit-stop (~60 ms) | thunder | heavy |
| Tesla fire | electric arc | zap | — |
| Tower damaged | hit flash + HP bar shake | impact | light (throttled) |
| Boss slam telegraph | ground ring decal grows | rumble build | — |
| Boss slam | shockwave + camera shake (big) | slam | heavy |
| Wave cleared | +3⚡ flourish | fanfare stinger | success pattern |
| Victory/Defeat | confetti / tower crumble | jingle | — |

## Implementation
- `JuiceDirector` subscribes to GameEvents, triggers pooled VFX prefabs + `ISfxService` + `IHapticsService`. Gameplay code never calls juice directly.
- Haptics: `Handheld.Vibrate` fallback; prefer fine-grained (e.g. Nice Vibrations-style or Android VibrationEffect / iOS CoreHaptics via simple wrapper). Throttle: max 1 haptic / 100 ms.
- Camera: single `CameraShaker` (amplitude-capped, additive sources).
- Audio: `SfxService` (pooled AudioSources, pitch jitter ±5%), `MusicService` (layered intensity: calm ↔ wave overlap count). AI-generate: music via Suno, SFX via ElevenLabs — log in AI Log.
- VFX: particle prefabs; AI-gen flipbooks/textures where useful.

## Acceptance
- Sound off / haptics off toggles work; no GC spikes from juice (pooled); a full run "feels" alive — every kill visibly pays.

## Notes / changes
- **18 Jul — Unified bomb-based blast family (user direction: RealisticHeavyBombBlast is THE blast).** All explosion impacts now derive from the Epic Toon FX bomb via prefab VARIANTS (inherit base tuning): `VFX_FireballBlastBig` (variant of `VFX_FireballBlastV2`, **exaggerated** — ×1.35 baked size+speed, ×1.8 embers/trails bursts, boom kept) wired as `SpellVfxDirector._fireBlast` in BOTH scenes, `_fireShake` 0.5→0.6; `VFX_CannonBlast` (variant of `VFX_MortarBlast`, ×0.5 compact, ×0.6 embers for mobile fire-rate, impact SILENT — muzzle already booms; audio muted via null clip since variants can't remove components) wired as `impactVfx` on `Projectile_CannonBall` + `Projectile_TowerCannon` (tints confirmed white). Mortar keeps its shipped `VFX_MortarBlast`. **Scaling fix baked into both variants:** children switched `scalingMode` Local→Hierarchy — before this, `VfxSpawner`'s runtime scale param (fireball spawns at `max(1.2, radius·0.6)` = 1.5) only grew the ROOT system while smoke/shockwave/scorch ignored it; now the whole hierarchy scales, so fireball's effective size ≈ 1.35 × 1.5 ≈ 2× everything. Non-blast hits (bolts/arrows/magic) intentionally keep their impacts. Verified live in TestRange: cast → meteor → big blast → pooled reuse (3 clones), dummy DPS registered, peak-frame captures of both variants, 0 console errors.
- **18 Jul — Damage-number popups (reference-video match).** Pooled 3D-TMP popups on every hit, replacing the old TextMesh `DamageNumbersView` (removed from the scene). `Scripts/Juice/`: `DamagePopup` (TextMeshPro digits + sliced `SpriteRenderer` plate — no world-space Canvas/rebuilds; per-popup material instanced ONCE so outline/underlay alpha can fade), `DamagePopupPool` (32 prewarmed — no Instantiate/Destroy in gameplay; saturated → recycle oldest active), `DamagePopupManager` (scene view-singleton, `Instance.ShowDamage(amount, worldPos, isCritical)`, single Update ticks all actives on WALL CLOCK, number-string cache, hashed spread/drift/stack-bump = no RNG). Anim: spawn 0.6× → ease-out-back punch 1.2× → SmoothStep settle 1.0× → ease-out rise + tiny lateral drift → text face + outline/underlay + plate all fade over the tail → pooled at ~0.7 s; camera-billboarded. Crits (enemy killing blows) = orange text + deep-red plate + 1.35× scale; rapid same-spot hits bump upward so they stack. Integration: `RoyalTower.TakeDamage` + `BuildingUnit.TakeDamage` call the manager the moment damage lands; enemies stay on the `EnemyDamaged` event (keeps DoT aggregation). Assets: `Prefabs/VFX/DamagePopup.prefab`, `Art/DamagePopupPlate.png` (generated rounded-rect, Full Rect + 22 px slice border), `Art/LatoBlack SDF DamageNumbers.asset` (Lato-Black SDF, white face / maroon outline 0.25 / underlay shadow); TMP Essentials imported. All tunables (timing/motion/scale/colors) on the `DamagePopups` GO. Bugs caught in verify: TMP measured while inactive → giant plate (activate before `GetPreferredValues` + clamp); stack-bump raised 0.32→0.5. Live-verified real ogre hit "125" over the roof + orange "325" crit; 0 console errors.
- **18 Jul — Tesla discharge v2 (Archero-reference match).** `ZapArcRenderer` rewritten: each zap = pooled 3-LAYER beam (thin white-hot core ×3.4 HDR + saturated electric-blue body ×~3 HDR + soft cyan glow on `Tex_SoftLine`) — three LineRenderers share ONE reused Vector3[] (endpoints track the victim EVERY frame; the jag shape re-rolls at a quality-controlled Hz), per-frame width/brightness flicker, one extra-sharp elbow per bolt, 1–3 hashed branch offshoots, and a `VFX_TeslaFlash` muzzle pop at the coil. `GameEvents.InstantShotFired` now carries the `IEnemyTarget` (not a point) so VFX can follow movers and attach impacts. NEW `TeslaTargetGlow`: ONE shared per-enemy impact (contact flash + cyan body glow + crawling arcs on a body-radius ring + outward sparks) that follows without parenting (survives pooling), sustains 1.35 s past the last zap, afterglows 0.28 s on disconnect, cleans up on death; extra simultaneous Teslas only raise its intensity (+22%/beam, capped) — never a duplicate. Per-Tesla contact points are hashed from the muzzle position around the victim's body radius, so 3 beams on one enemy stay readable. **Quality tiers** Low/Med/High on `ZapArcRenderer.Quality` (segments 7/10/14, branches 1/2/3, crawl arcs 1/2/3, sparks ×0.6/0.8/1, jag refresh 20/30/45 Hz) — TestRange has a cycle button; Game scene ships Medium. **Bloom** added (both scenes): URP Volume, threshold 1.2 / intensity 0.4 — only the HDR beam layers bloom; hit-glow tints lowered to ≤1.55 so enemy flashes never blow out; verified good with bloom OFF too. Perf: beams/glows/flash all pooled (beam pool stabilized at 3 under sustained triple-Tesla fire), zero per-frame allocations in the hot path, point arrays reused, branch scratch shared. Test matrix passed: 1 Tesla/1 target, 3 Teslas/1 target (one glow), multi-target, moving target (endpoint tracked exactly), death mid-attack, rapid retarget, bloom on/off, pooled reuse — 0 console errors. Gotchas logged: URP `Particles/Unlit` materials must be configured like `Mat_ParticleAdd` (keywords incl. `_FLIPBOOKBLENDING_OFF` — hand-setting floats alone renders as untextured squares; `Material.mainTexture` does NOT map to `_BaseMap`); pooled beams must be styled at spawn or they wear last life's faded widths for one frame.
- **17/18 Jul (overnight) — FULL JUICE PASS.** Everything below verified live in TestRange + Game scenes, 0 console errors.
  - **Spell choreography** (`SpellVfxDirector` + pooled `SkyfallEffect`): Arrows = 15 real arrow meshes falling onto a deterministic golden-angle spiral inside the ring (stagger 0.14 s, stick 0.5 s, dust pops on landing); Fireball = meteor with fire trail streaking down + `VFX_FireballBlast` (FireImpactMega + fireimpact.wav) at landing; Freeze = frost nova + lingering `VFX_GroundFrost` patch + frozen units tint ice-blue AND hold their pose (animator speed 0 while blocked); Lightning = the STORM (`LightningStormView`, 17-Jul): on cast the HUD `ThunderSplash` image (highlight-white sprite, preset cyan-blue color) pulses its alpha 0 → 57/255 → 0 with the storm envelope — scene LIGHTING IS NEVER TOUCHED and nothing darkens (user ruling 17-Jul after two darker iterations); a runtime URP Volume weight-blends in a bloom boost (2.2) only; at resolve (0.35 s `fallDelaySeconds`) Tesla-style 3-layer jagged bolts (same Mat_TeslaCore/Body/Glow, ~1.7× wider) crash down from BEYOND THE TOP SCREEN EDGE (origin projected to viewport y=1.18 at the contact's camera depth, hashed horizontal drift — user ruling: a bolt starting mid-screen looks bad) onto each actually-struck victim (positions via `SpellContext.HitReport` → `GameEvents.SpellResolved`) with a bigger Tesla-like impact (contact flash, spark burst, crawling ground arcs), rising alpha-blended smoke (`Mat_SmokeSoft`), a white thunder-crack screen flash and 0.5 camera trauma; then dawn fades back over 0.55 s. Old `VFX_LightningPillar` retired. Two-event model: `SpellCast` starts the telegraph, `SpellResolved` fires the payoff exactly when damage lands. `SkyfallEffect` follows the game clock (IClock) so slow-mo/fast-forward stay in sync.
  - **Camera:** `CameraShaker` (trauma², Perlin, applied/removed per-frame in LateUpdate, capped) on both scene cameras — fireball 0.5, lightning 0.35, boss slam 0.55, building place 0.15, freeze/arrows ticks. `CameraAspectFitter` keeps world WIDTH constant across aspect ratios (reference 1080×1920).
  - **Enemy lifecycle** (`EnemyLifecycleView`, auto-added by EnemyAgent): spawn ease-out-back scale pop + spawn dust (new `EnemySpawned` event); death = anim reads first, then `RoyalSiege/Dissolve` shader burn-out (URP unlit, UV value-noise, ember edge; materials swapped per-instance, restored on pooled respawn); freeze tint + hit glow COMPOSE on one MaterialPropertyBlock (HitReaction routes its pulse here when present). Per-enemy `deathVfx` on EnemyDefinitionSO — Skeleton (Enemy_Mage) gets `VFX_BoneBurst` (13 tumbling bone-white capsule mesh particles, gravity, shrink-out). Boss slam raises `BossSlammed` → dust burst + big shake.
  - **Tesla:** zap now leaves the coil TOP (Muzzle child at y 1.18; StructureAttack stops adding +1 up when a firePoint exists) and each shot is a main bolt + 2 deterministic hashed FORKS (shorter life/width) — reads as real thunder.
  - **X-Bow:** `_yawModelOffsetDegrees` 90 → **-90** (mesh visual front is +X, it fires -X — it used to aim correctly but LOOK backwards); new `BowStringView` — 3-point LineRenderer between limb-tip anchors, middle pulled back while the arrow is nocked, snaps forward on release (asymmetric spring: slow draw, fast snap).
  - **Cards** (per the card-animations spec, all runtime-built, no scene rewiring): same-frame border+glow on touch, 1.08 ease-out-back pop + 10 px lift (on a runtime "Lift" container so the HorizontalLayoutGroup never fights), zero-lag `CardDragProxy` whose scale is position-driven (100%→50% toward the board), vacated slot = recessed dark panel + name watermark (slots never reflow), deploy = proxy dissolve 120 ms + `FloatingCardLabel` name drift, cancel = tween home 180 ms, refill pop 0.8→1.05→1.0, unaffordable = desaturated art + colored cost badge + decaying shake on tap.
  - **Grid** (`GridOverlayView`): additive white 0.5-unit grid quad (procedural texture, radial fade baked) over the deployment circle, shown only while dragging a BUILDING; grass checker (4-unit repeat, origin-aligned) = exactly 8 snap cells, so texture and grid read as one system. Placement verified landing on exact 0.5 multiples.
  - **MagicArsenal imports** via `Tools/import-magic-arsenal.ps1` (GUID closure copier — reusable): Fire projectile/ImpactMega, LightningPillarBlast, FrostSphereBlastV2, GroundFrost + sounds; 9 legacy materials auto-converted to URP Particles/Unlit. All library blasts were authored huge → global shrink pass (sizes ×0.32–0.45, speeds ×0.42–0.5, shapes ×0.4–0.5; lightning ×0.6 again).
  - **Deliberately skipped:** post-processing (bloom/vignette) — additive materials already read well and low-end mobile is a target; revisit only if Day 3 has perf headroom.
- **17 Jul — king spell "shooting star" pass + hit reaction.** `Proj_KingMagic` rebuilt as one continuous motion: TrailRenderer is the hero (0.32→0 taper, dense verts, round cap, white→ice-blue→deep-blue), FrostTwinkle = small star dust emitted over DISTANCE (traces the path exactly; zero distance = zero particles, so nothing can flash before launch), FrostSpikes converted from an impact-like outward burst into a tight local-space twinkling star head. `Projectile` now hard-resets ALL pooled FX on Launch (trails cleared, particles Clear+Play, light re-enabled) and on impact/fizzle stops emission and lingers until the ribbon fades instead of cutting it mid-air. New `HitReaction` (RoyalSiege.Juice, view-only): 0.22 s backward recoil pulse + overbright glow flash via MaterialPropertyBlock on every renderer; auto-added to enemies (EnemyAgent) and the test dummy; cancelled on death/pool reuse. Contract: owners must rewrite transform.position every frame (EnemyAgent + TestDummyTarget do) so the LateUpdate offset never accumulates. King now casts from the actual `RightHand` bone: `KingView.CastPoint` (auto-found by name) is the king attack's `firePoint`; `Projectile_KingMagic.spawnHeightOffset` 1.7→0; clip sampling confirmed impactFraction 0.4 = the hand-extended release pose, so timing and spawn point match the throw.

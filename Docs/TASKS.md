# TASKS — living progress tracker

Legend: ⬜ todo · 🔄 in progress · ✅ done · Update after EVERY task. Keep newest notes at top of the log section.

## Day 1 (16 July) — CONTROLLER: playable vertical slice by end of day

- ✅ Read hackathon brief, GDD v2, mocks, meeting notes; set up all Docs/ + CLAUDE.md + AI Log
- ✅ M-Data: all SO classes + authored assets (7 cards, 4 enemies, 5 projectile settings, 4 effects, configs, deck, 12-wave timeline) in `Assets/TowerDefense/Data/`
- ✅ M-Core: GameContext (composition root), TickSystem (10 Hz + speed/pause), GameEvents, ObjectPool, RangeMath, WinLoseEvaluator
- ✅ Arena scene: circular ground r15, deployment ring r5, map edge ring, 8 spawn points, portrait camera, Royal Tower placeholder — `Scenes/Game.unity` wired & saved
- ✅ M-Enemies: EnemyAgent (priority targeting + hysteresis, melee/ranged, statuses, boss slam), pooled EnemyFactory, WaveScheduler (absolute times, formation offsets)
- ✅ M-Buildings/Combat: StructureAttack (targeter+cycle+delivery), shared Projectile module (arc/height configurable), RoyalTower (2 attacks), RangeRing
- ✅ M-Economy: EnergyBank (no regen, hard cap, waste event) + orb flight credit-on-arrival + unconditional wave bonus
- ✅ M-Cards: DeckService ring buffer (single-shuffle RNG), per-card cooldowns, CardPlayService (validate at release)
- ✅ M-Spells/Placement: SpellEffectSO strategies (AreaDamage/Freeze/Lightning), SpellCaster, PlacementValidator, ghosts, PlacementController + temp DebugInputDriver (keys 1–4 + mouse)
- ✅ Slice smoke test IN PLAY MODE: wave 1 goblins spawned→walked→died to tower; orbs credited (capped at 10 correctly); X-Bow placed via real drag flow (energy 10→4, hand cycled, NEXT correct)
- ✅ Full-run verification at 4× speed: ran to wave 10 with one X-Bow, tower died → MatchEnded(defeat) fired, clock auto-paused. 0 console errors/warnings entire run
- ⬜ Verify remaining acceptance cases: pure-idle dies ~wave 4, ogre buildings-first, mage stops at siege range, freeze/lightning/fireball behavior, victory + stars path
- ✅ Greybox HUD: hand bar (4 slots + NEXT, cost pips, radial cooldowns, affordability grey-out, 40 px drag threshold), energy bar (magenta, gold pulse at cap), wave banner + clear bonus flash, victory/defeat panel with stars. Screen Space Overlay canvas, 1080×1920 reference, EventSystem + InputSystemUIInputModule
- ✅ Animations: Goblin/Skeleton FBX rigs converted Generic→Humanoid (avatars verified human), shared `AC_Unit.controller` (Walk/Attack/Die; state speed driven by MoveSpeed/AttackSpeed params) assigned to all 3 enemy prefabs. UnitAnimator auto-reads attack clip length → swing/damage stay in sync. Verified in play mode: Walk→Attack states, facing correct
- ✅ Locomotion blend tree (17 Jul): Walk state motion replaced with 1D "Locomotion" blend tree on new `MoveBlend` param — 0 = Walk clip @0.08× (near-still idle sway; no Idle clip exists in the pack), 1 = full walk. Fixes the hard mid-stride freeze on stop; `MoveSpeed` state-speed param kept for exact foot-speed matching (parked at 1 while stopped so the sway plays). Attack entry crossfade 0.15→0.2 s. ⚠️ Not yet play-verified (edited outside the editor)
- ✅ Per-enemy AnimatorOverrideControllers (17 Jul): AOC_Ogre / AOC_Goblin / AOC_Skeleton / AOC_DoubleHorn created over `AC_Unit` and wired into each enemy prefab's Animator — all four override Walk/Attack/Die with Enemy 2's mixamo clips for now; per-character clips later = swap 3 slots per AOC. Enemy 4 rig flipped Generic→Humanoid + controller wired (had none). ⚠️ Not yet play-verified
- ✅ Anim sync fix (17 Jul): enemies looked static — `UnitAnimator` damped MoveBlend/MoveSpeed from the 10 Hz tick with frame-dt (~0.16 s of damping progress per real second, blend stuck near the 0.08× sway), and Rebind reset MoveSpeed/AttackSpeed to controller default **0** (fully frozen pose on pool reuse). Fixed: targets cached in SetMoving, damping integrated per-frame in UnitAnimator.Update; Rebind snaps MoveBlend=0/MoveSpeed=1; controller defaults MoveSpeed/AttackSpeed = 1. Transitions smoothed: Die AnyState transition ordered FIRST + interruption source Source on all three transitions (death now cuts into an in-progress attack crossfade), Attack entry 0.25 s, Die 0.3 s. Also: death anim now plays when a unit dies while frozen/stunned (playback speed restored in OnDied). ✅ Play-verified 17 Jul (waves 1–6 live: enemies march at full walk immediately, packs spread, combat clean, 0 console errors/warnings)
- ✅ Hurt/hit-stagger mechanic (17 Jul): a MOVING enemy that takes damage stops and plays a hurt flinch, then resumes walking. No hurt clip exists in the pack → new `Hurt.anim` = copy of Die 2 (opening backward flinch), Hurt state plays its first 35% speed-scaled to fit the stagger, then crossfades back to Walk (0.15 s in / 0.25 s out; walk params intentionally untouched during the stagger so the exit lands mid-stride). AnyState priority: Die → Hurt → Attack, interruptible. Gameplay side gated in EnemyAgent: only while moving (never attacking/frozen), per-enemy `hurtStaggerSeconds` + anti-stunlock `hurtCooldownSeconds` in EnemyDefinitionSO (0.35/2.5 all, boss 0.3/6 — boss under sustained fire must not perma-flinch). ✅ Ran clean in a live play session 17 Jul (waves 1–6, staggers firing under king fire, 0 warnings/errors); flinch READ not yet eyeballed frame-by-frame — swap the Hurt.anim slot per-AOC when real hurt clips arrive
- ✅ Per-hit hurt (17 Jul, user request): every hit on a WALKING enemy now replays the flinch — `hurtCooldownSeconds` 0 for Goblin/Mage/Ogre (boss kept 6 s anti-flinch-lock), mid-stagger hits restart the stagger, AnyState→Hurt allows self-retrigger. Deterministically verified in-editor (hit → Hurt, mid-flinch hit → Hurt>Hurt restart, post-recovery hit → Walk>Hurt). Attacking/frozen units still never flinch. ⚠️ Balance: massed towers can now near-lock a walking ogre — Day-3 balance pass should watch this (knob: per-enemy hurtCooldownSeconds)
- ✅ Animator param-guard + MCP session fix (17 Jul): AC_King spammed "Parameter 'Hash -1142910944' does not exist" — that hash is CRC32("MoveBlend"): the shared UnitAnimator's new per-frame damping drove MoveBlend/MoveSpeed on EVERY animator, but AC_King lacks MoveBlend (and Hurt). UnitAnimator now caches the controller's parameter set in Awake and gates every Set* call on it — safe on any rig. Play-verified live: 0 warnings across waves 1–6. Also fixed Unity MCP for future sessions: config only existed under the forward-slash project key in ~/.claude.json → added repo-root `.mcp.json` (UnityMCP → http://127.0.0.1:8080/mcp). NOTE: the hub server must be running — start from the MCP for Unity editor window, or `uvx --from mcpforunityserver mcp-for-unity --transport http --http-url http://127.0.0.1:8080`
- ✅ Grass ground per mocks: `grass pattern.png` (wrap=repeat) on a 36×36 plane, 9×9 tiling
- ✅ Real tower model (`Environment/Tower`) swapped in for the grey cylinder, auto-scaled to ~3.2 tiles
- ✅ World-space health bars: enemies + buildings (hide when full) + tower (always visible), billboarded, color lerps green→red, driven via `IHealthReadout` (UI polls gameplay; gameplay never references UI)

- ✅ Crowd/animation polish: arc-spread staggered spawn formations, per-unit body radii + separation steering (no overlaps — audited), velocity-matched walk anim (no foot-slide), de-synced walk phases, smooth turns, blended animator transitions
- ✅ Structure-overlap clamp (17 Jul): enemies could clip INSIDE the tower while attacking (seek overshoot on the 10 Hz tick + crowd separation shoving ring-attackers inward). `EnemyAgent.ResolveStructureOverlap()` now clamps every unit's centre to a standoff ring `footprint + min(bodyRadius, 0.9×attackRange)` around all nearby structures after each movement tick — the 0.9 cap keeps fat units (Ogre body 0.85 > range 0.8) attack-capable. Also stops enemies walking through placed buildings. Play-verified: 4 live audits over waves 1–5 (13 enemies, 11 sieging) — zero footprint penetrations, attackers ring the tower base exactly at standoff

- ✅ Full-bleed ground: map-edge ring hidden (visual only — the r=15 gameplay boundary lives in config), ground plane 200×200 with density-preserving 50×50 tiling, camera clears to grass green — no skybox on any phone/tablet aspect

## Day 2 (17 July) — CONTENT

- ✅ Building attack juice: BuildingTurret (cannon Octagone yaw-tracks target 420°/s, Body anticipation+recoil; X-Bow fast swivel 720°/s + snappy recoil; Tesla charge-squash→discharge-pop; all buildings ease-out-back pop-in on placement)
- ✅ Projectile art + juice: metallic cannonball (spin+smoke trail), Meshy Arrow bolt (tracer), purple emissive mage bolt; per-settings muzzle flash + impact burst (tintable); trajectory heights/arcs raised so nothing clips the tower
- ✅ VFX pipeline: pooled VfxSpawner + JuiceDirector (death puffs, placement dust, per-spell tinted bursts) + Tesla ZapArcRenderer (animated jagged arcs + sparks). All verified live: pools show MuzzleFlash/Impact/Puff/SpellBurst/Sparks all spawning, turret yaws confirmed tracking, 0 console errors

- ⬜ All 7 cards implemented (Tesla instant, X-Bow, Fireball, Freeze, Lightning + statuses)
- ⬜ All 4 enemies (Mage ranged+splash, Ogre buildings-first, Boss + slam) with priority/hysteresis rules
- ⬜ Full 12-wave timeline + WaveCleared/victory/stars/defeat + end screens
- ⬜ Full HUD per 10_MODULE_UI (NEXT preview, banners, pause 3-2-1, speed toggle)
- ⬜ Environment 1 final (meadow) + **Environment 2** (second biome — decide theme)
- ⬜ Character models hooked with shared animations; dummy → real swaps where assets arrive
- ⬜ Balance pass 1: full runs; check idle-dies-wave-4, winners at 40–70% tower HP
- ⬜ Mobile build on device; touch + performance check

- ✅ **TestRange scene** (`Scenes/TestRange.unity`): isolated tuning sandbox — grass floor, full tower+king, wandering auto-heal dummy target with health bar, runtime button column (spawn each building/enemy, clear, kill all, wander toggle, HP reset, 0.25×/1×/3× speed for eyeballing trails), live Target DPS readout. `TestRangeContext` is a minimal composition root (no waves/economy) — spawnable lists are inspector-editable. Odin Inspector 3.1.14.2 imported for nicer inspectors
- ✅ King on the Royal Tower: Golden King (default scale) with AC_King (idle sway + speed-synced throw), turns to target, fires magic orb (dual trail + sparkle wake) with 5-layer arcane impact splash
- ✅ King spell "shooting star" pass: trail is one continuous ribbon (star head + distance-emitted dust tracing the path), pooled projectile FX hard-reset on launch + trail fades out at impact instead of cutting; new view-only `HitReaction` on all enemies/dummy (backward recoil that settles + glow flash on every hit); king casts from the actual RightHand bone (`KingView.CastPoint` → `firePoint`, spawnHeightOffset 0) with release timing verified against the throw clip (impactFraction 0.4 = hand-extended pose)

- ✅ **Overnight juice pass (17→18 Jul)** — see 11_MODULE_JUICE notes for full detail: sky-fall spell choreography (arrow volley / meteor+blast / freeze nova+tint+pose-hold / lightning pillars on actual victims, damage synced to landing via `fallDelaySeconds`), camera shake (trauma-based) + aspect-ratio fitter on both cameras, enemy spawn pop + death dissolve shader + skeleton bone-burst, Tesla coil-top forked thunder, X-Bow facing fix + live bow string, full card-animation spec implemented (select/drag-proxy/dissolve+name-float/cancel/refill/unaffordable-shake/recessed-slot), placement grid overlay synced with the grass checker, TestRange artist tools (cast-spell buttons, FX gallery replay buttons, serialized lists), first spell SFX wired (fire/lightning/frost impacts). All verified live, 0 errors.
- ⚠️ **Balance observation (18 Jul, from unattended run):** near-idle play (one fireball only) reached DEFEAT at wave 7/12 — pressure curve is alive but the wave-4 idle-death invariant should be re-verified by a human run on Day 3 (king 93 DPS folded from tower may have softened early waves).

- ✅ **QA round 1 — Coda bug sheet "Defence Tower-Bug Sheet-Build#(17-07-2026)"** (full detail: `QALogs/QA_Log_Build_17-07-2026.md`): 11 bugs triaged → 10 fixed, 1 confirmed by-design (DT-003 4-tower cap, GDD §3). Headline root cause: the spell ghost's range circle rendered at 60% of the true radius (0.6-scaled parent shrank the RangeRing) — behind all four "spell hits outside its radius" High bugs; RangeRing now scale-proof (world-space vertices). Also: rings clip to the map circle, player projectiles clamp flight+impact to their attack radius, tower/king targeting honours the map boundary (spawn-zone units untargetable), sky-fall damage re-checks the radius at landing, match-end cancels card selection + locks the hand + panel forced topmost, unaffordable cards can't enter placement, meteor drop straightened. Statuses + notes written back to the Coda sheet; design deltas in Docs/01.

- ✅ **Lightning "storm" rework (17 Jul, user brief: mighty effect)** — `LightningStormView`: cast → the HUD `ThunderSplash` image (highlight-white sprite, preset cyan-blue) pulses alpha 0 → 57/255 → 0; scene LIGHTING NEVER TOUCHED, nothing darkens (final after 3 user iterations); runtime URP Volume adds a bloom boost (2.2) only. 0.35 s later Tesla-style 3-layer sky bolts (~1.7× Tesla width, same materials) crash down from BEYOND THE TOP SCREEN EDGE (origins projected to viewport y=1.18) onto each victim with bigger Tesla-like impacts (contact flash, sparks, crawling ground arcs) + rising smoke (`Mat_SmokeSoft`), white thunder-crack screen flash, 0.5 shake. Card_Lightning `fallDelaySeconds 0→0.35` (delta in Docs/01 — targets captured at cast, radius re-checked at strike). Old `VFX_LightningPillar` retired. Screenshot-verified, splash/bolts/pools all restore clean, sun stays at 1.0 throughout.

- ✅ **PROGRESSION V4 IMPLEMENTED (17 Jul evening)** — full two-stage journey per `Docs/13_PROGRESSION_V4.md`: 44 clear-triggered waves (2 s gap after last enemy + last orb; no timers), 7-enemy roster on existing rigs (Sk1/Sk2→Skeleton, G1/G2→Goblin, H1/H2→DoubleHorn, Ogre; type-2s scaled 1.15×), 10-card roster (NEW: Log line-roll with knockback + wind-up interrupt, Earthquake DoT/slow zone, Knights player-side melee troop; Freeze→"Frost-ball"; Fireball +knockback; Lightning 300), building HP decay (maxHP/lifetime per sec, HP bar = lifetime bar), hybrid elixir economy (start 5, +1/kill orb, Ogre +5, passive 1/3.5 s, wave bonus removed), 5 tower levels (full heal on level-up, king damage 93→155), 5 checkpoints with level-up/stage-complete screens (full sim pause, card unlock into queue back) + `royalDefense.profile.v1` save + defeat→RETRY-from-checkpoint (scene reload restores per §0.5), stage header UI. JUICE: procedural ground-crack decal + dust/pebbles/rumble for Earthquake, rolling log with dust wake + break-apart, knight spawn pop/lunge/clash sparks/death puff. Play-verified end-to-end: wave 1→checkpoint 1 (level-up screen, Fireball unlock, save), retry restore exact, decay 20/s exact, Log one-shots Sk1 (light tier ✓), Earthquake DoT+slow visible, knights chase/fight, 0 console errors. **Balance NOT yet verified beyond wave 5** — §10 harness targets (idle-loss per level, Ogre waves, full-campaign winrate) are a Day-3 pass. Old v2 assets (Deck_Default, Waves_Level1, Enemy_Goblin/Mage/Ogre/OgreWarlord) retained but unused.

- ✅ **Tower range honesty + Fireball VFX rework (17 Jul, user pass)** — king range 7→5 so the Royal Tower can never hit outside the drawn white circle (live-verified: worst target 4.71 across 61 combat samples; buildings keep card ranges; balance warning in 13 §7). Fireball root cause: MagicArsenal fire materials carried the legacy 0.5-gray tint (legacy shaders doubled it, URP doesn't) + premultiply blend → everything at half brightness = the "missing texture" look. All 5 fire mats fixed (additive full-bright / smoke alpha full), tinted hot-orange, meteor 1.4× from height 9.5 with white-hot→deep-orange trail, blast ×1.5 + heavy 14–20-plume ember smoke lingering ~2.5 s. Screenshot-verified fall/blast/smoke; 0 errors.

- ✅ **Knights guard rules + no building cap (17 Jul, user passes)** — knights: guard the tower's white circle (target only enemies inside it, hard leash every tick, wait for targets to enter — verified max dist 4.05, zero out-of-circle chasing) and can't walk through the tower/buildings (standoff clamp; forced-inside knight ejected to exactly 1.90). Building count cap REMOVED (`maxPlayerBuildings` 0 = unlimited; supersedes max-4 + QA DT-003 — Coda updated; verified 6 towers placed). Space/overlap/elixir/decay are the only placement limits now.

- ✅ **Real-art HUD pass (17 Jul)** — swapped the dummy procedural HUD for the imported UI kit, pixel-matched to `UI/mock/mock_1.jpg`: imported all UI PNGs as sprites; assigned each card's `icon` = its thumbnail; rewrote `CardSlotView` to render card art behind the blue `Frame` with a lightning cost badge + radial cooldown + selection glow (anims kept); rebuilt the scene HUD (full-width cream banner, Next Up deck slot, 4 card slots, gold elixir bar with `icon_mana` gem, top Stage title + green progress bar); wired all serialized refs incl. EnergyBarView gold colors. Play-verified (cards play, desaturate when unaffordable, elixir/stage bars update, 0 errors). Note: elixir is a smooth gold fill (mock segments optional later); older boar-mock's pause/coin/FF buttons omitted per the cleaner mock_1. **Env gotcha:** two Unity editors were connected — pin `set_active_instance TowerDefense-AIThon` when that happens.
- ✅ **Tower level visuals + ruin wired into the game (18 Jul)** — root cause: `Game.unity` still instanced the OLD `RoyalTower.prefab` (no `TowerLevelView`), so the 4 level models + fail model built into `RoyalTower_Full.prefab` never appeared. Scene instance swapped to `RoyalTower_Full` (same transform/name, `GameContext._royalTower` rewired, scene saved). Code: `TowerLevelView.SetLevel(level, instant)` + `RoyalTower.ApplyLevel(..., instantVisual)` — resume/retry loads snap the earned level with no surge/shake; `SetLevel` guarded after fail (ruin is terminal). Play-verified: resume showed L2 instantly (HP 4600), ApplyLevel(3) played the seamless swap to the L3 model, tower death hid all levels + King and crashed in the ruin, post-fail SetLevel no-op, 0 console errors. ⚠️ 5 config levels vs 4 models — L5 reuses the L4 model (asset gap logged in 12_ASSET_STATUS).

- ✅ **Old tower = Level 1 + scene re-swap + HUD prefab-ization repair (18 Jul)** — user request: keep the previous tower model in the current scene. `RoyalTower_Full.prefab`: old `TowerModel` inserted as `_levelModels[0]` (removed from `_hideOnInit`) → L1 = old tower, the 4 new fortress models = L2–L5 (5-model gap CLOSED, every config level now has a distinct model). Scene had been reverted to the old `RoyalTower.prefab` → re-swapped to `RoyalTower_Full` + `GameContext._royalTower` rewired + saved. ALSO repaired collateral from the new `Prefabs/UI/HUD.prefab` extraction (prefab assets can't hold scene refs): all 5 HUD views' `_context` → scene overrides to GameContext, `HandBarView._placement` → PlacementController, `LightningStormView._thunderSplash` → HUD instance's ThunderSplash, `EnergyBarView._label` wired inside the prefab (was the NRE storm on every play). NextSlot's null `_glow/_cooldown/_cost*` are by-design (new skin, CardSlotView null-safe). Play-verified: resume L2 shows fortress-1, SetLevel(1) shows the old tower with King, upgrade anim OK, real `WaveCleared(4)` checkpoint → level-up screen + pause + save byte-identical to the player profile, 0 console errors/warnings.

- ✅ **Hellspawn throw = AnimationEvent-released (18 Jul)** — user request: fire the attack at the throw clip's release frame. New `AnimationEventRelay` (on the Animator's model GO — Unity only delivers clip events there) forwards `AnimEvent_Throw` to `EnemyAgent`. New data flag `EnemyDefinitionSO.attackImpactOnAnimEvent` (true on Hellspawn1/2): the 10 Hz `AttackCycle` still schedules the attack (arms a pending impact at impactFraction), but the LAUNCH waits for the clip event — bolt leaves the hand exactly on the release pose; 2×attackRate timeout fallback so a culled animator never drops the hit. Event added via the Throw.fbx ModelImporter at the measured release frame (t=0.867s = 39%, hand max-forward sample), `DontRequireReceiver` (AnimationTester-safe). Root-cause bonus: the clip was named `mixamo.com` so UnitAnimator's attack-length auto-detect missed it → renamed importer clip to "Throw Attack" + re-pointed AOC_DoubleHorn → AttackSpeed now 1.57 (one throw per 1.4s cycle, was one per 3.1s = half DPS). Live-verified: events every 1.40s exactly at nt=0.39–0.40, launch+held-orb cycle on the event, 0 console errors/warnings.

- ✅ **Enemies face the tower while attacking (18 Jul)** — Ogre (worst case) visibly turned away mid-attack. Gameplay facing was already perfect (wrapper transform tracks the target at 0° error) — the twist was BAKED INTO THE CLIPS: every attack FBX had "Root Transform Rotation → Bake Into Pose" ON, so the source animation's root rotation (up to 131° on the Ogre's mixamo combo) played through the bones. Fix: Bake Into Pose OFF + Based Upon = Body Orientation on all four attack FBXes (Ogre combo, HumanM@Attack1H01_R, HumanM@Attack2H01, Throw.fbx) — rotation extracts to root motion, which root-motion-off discards; the body stays locked on the agent's facing. Bonus: Ogre clip renamed `mixamo.com` → "Ogre Attack" (+ AOC_Ogre re-pointed) so UnitAnimator's attack-length auto-detect works. Live-verified with a chest-yaw probe: Goblin ±13°, Ogre ±14° (brief −25° combo windup — natural), was ±85–131°; Throw.fbx's AnimEvent_Throw survived the reimport; 0 console errors/warnings.

- ✅ **Ogre attack 35% slower + king centered on every tower roof (18 Jul)** — (1) new per-enemy `EnemyDefinitionSO.attackAnimSpeedMultiplier` (0.25–4, default 1, mirrors the walk multiplier; applied AFTER the period-fit clamp in `UnitAnimator.PlayAttack`, purely visual — attackRate/impactFraction untouched); Ogre = 0.65 → live AttackSpeed 1.6→1.04. (2) King placement: `TowerLevelView.AlignKing` sets KingRoot's **Y only** to the active model's measured roof top (+`_kingHeightOffset`); X/Z stay exactly as authored on the prefab (user ruling — an XZ-centering variant was tried and reverted on request); the upgrade glide lerps Y. Fail state untouched (king hidden). Verified: kingRoot (0, y, 0.56) with y = 3.40/2.88/2.35/2.52 across L1–L4, 0 console errors/warnings.

- ✅ **Damage-number popup system (18 Jul)** — pooled, TMP-based popups on every hit (tower + buildings via `TakeDamage` → `DamagePopupManager.Instance?.ShowDamage(amount, pos, crit)`, enemies via the existing `EnemyDamaged` event; killing blows = crits). New `Scripts/Juice/`: `DamagePopup` (3D TextMeshPro + sliced SpriteRenderer plate, per-popup material instanced ONCE for outline/shadow fade), `DamagePopupPool` (32 prewarmed, zero Instantiate in gameplay; saturated → recycle oldest), `DamagePopupManager` (scene view-singleton, single Update ticks all actives on wall clock, hashed jitter — no RNG, number strings cached). Anim: 0.6→1.2 punch (ease-out-back) → settle 1.0 → rise + drift → text+plate fade, 0.7 s, all tunables serialized. Assets: `Art/DamagePopupPlate.png` (generated rounded rect, sliced), `Art/LatoBlack SDF DamageNumbers.asset` (digits baked; white face, maroon outline 0.25, underlay shadow), `Prefabs/VFX/DamagePopup.prefab`; `DamagePopups` GO in Game.unity; old TextMesh `DamageNumbersView` removed from the scene (file kept). TMP Essentials imported. Live-verified: real ogre hit shows "125" over the tower roof, crit shows orange "325" on deep-red plate (close-up), rapid same-spot hits stack; 0 console errors/warnings.

- ✅ **Knight walk anim slowed + synced (18 Jul)** — user: knight walk too fast, slow it and sync, KNIGHT ONLY. Root cause: KnightUnit never called `ConfigureWalk`, so its walk multiplier stayed 1.0 → the foot-synced cadence at moveSpeed 1.6 with the short-stride (0.645 u/s) clip is 2.58× = frantic. Fix (knight-only): new serialized `KnightUnit._walkAnimSpeedMultiplier` (0.6, Range 0.25–1) applied via `_animator.ConfigureWalk(...)` in `Init` — mirrors the enemies' per-def walk knob but nothing else reads it. Cadence 2.58×→1.55× (40% slower, calm march); the tradeoff is ~38% foot-slide, unavoidable since true grip *requires* 2.58× (tunable via the field). Transitions already smooth (AC_Knight untouched): live probe shows MoveBlend ramps 0→0.6→0.87→1.0 on start / 1.0→0.86→0.27→0 on stop and MoveSpeed damps 1.0↔1.55 with no snaps. 0 console errors.

## Day 3 (18 July) — POLISH & SUBMISSION

- ⬜ Juice map implemented (11_MODULE_JUICE): VFX, SFX, haptics, camera shake, hit-stop — **large part done overnight (VFX + shake + first SFX); remaining: haptics, hit-stop, music, full SFX set**
- ✅ Music + SFX integrated (18 Jul): all 17 chosen sounds from `Assets/TowerDefense/Sounds/` wired (BGM loop, splash drum, flow stings, spells, per-enemy weapon hits, X-Bow, knight, revive — see 12_ASSET_STATUS Audio). Remaining dummies: enemy-hit thock / death grunts / tower-hit / building-placed / knight-spawn (still Feel demo clips — no replacements provided)
- ⬜ UI skin pass (AI-generated card frames/icons)
- ⬜ Balance pass 2 + bugfix
- ⬜ 2–3 marketing creatives (AI)
- ⬜ 5-minute video (concept, gameplay, AI workflow)
- ⬜ AI Log final review + submission

## Change log (newest first)

- **18 Jul (latest)** — **ENDLESS 2-STAGE LOOP + PER-STAGE ENVIRONMENT + REAL SOUND PASS** (user brief). (1) Loop: both stages verified authored (S1 w1–20, S2 w21–44); new `CampaignSO.loopStages` (=1) makes `WaveScheduler` wrap w44→w1 forever (S1→S2→S1→…); new 6th checkpoint after w44 (stage-complete, stars). `CheckpointService` made loop-safe: tower never downgrades (max(checkpoint, current) + full-heal kept), unlocks never duplicate (profile-gated), repeat level-up checkpoints are SILENT (save+heal, no modal/pause), stage-complete screens always show, profile `nextWave` wraps (44→1). Victory can't fire while looping (defeat = only end). (2) Environment: new `StageEnvironmentView` on `Arenas` toggles `Arena`/`Arena_Stage2` per stage — applies at load via `WaveScheduler.PendingStageIndex` (resume boots the right biome), on every WaveStarted, and pre-swaps behind the stage-complete modal. (3) Sounds: all 17 chosen .ogg in `Sounds/` integrated (see 12_ASSET_STATUS Audio) — GameLoop BGM loop (`Music` GO), splash drum, level-up/stage-victory/tower-destroyed stings, card-play woosh, spell set (arrows/fireball/freeze/quake/log) via new FeedbackDirector SpellCast/SpellResolved subs, per-enemy weapon hits via new `EnemyDefinitionSO.attackHitSfx` + `EnemyAttackImpact` event (skeletons/axe-goblin/spear-goblin), knight sword via KnightStruck, X-Bow via `VFX_XBowMuzzle` AudioSource, Revive on a DontDestroyOnLoad temp source. PLAY-VERIFIED live: boot at w44 resumes into Stage2 env → w44 kill → "Stage Complete!" modal + env pre-swap to Arena + profile nextWave=1 → Continue → w1 fights on Arena (CampaignComplete=false) → w1–4 cleared → repeat w4 checkpoint SILENT (no modal, tower stayed L5, 70%→100% heal, no dup Fireball, deck 10) → separate run: w20 clear → modal + swap to Stage2 → w21 fights on Stage2 → tower killed → defeat screen → Revive → reload boots Stage2 at L3 full HP. 0 console errors/warnings across all runs; test profiles restored to the user's real save afterwards. ⚠️ Editor gotcha: with `runInBackground=false` an unfocused editor freezes play mode — MCP sessions must set `Application.runInBackground=true` before driving tests.

- **18 Jul (latest)** — FIXED game-frozen/no-waves deadlock: `CheckpointScreen` + `MatchEndScreen` roots had been disabled via SCENE overrides in Game.unity (HUD.prefab was fine) → views never subscribed → checkpoint pause waited forever for a Continue tap on a screen that couldn't show. Reverted both `m_IsActive` overrides; prevention: Checkpoint `Panel` now saved inactive in its prefab so the screens render nothing in edit mode. Verified: boot-resume runs wave 5 with enemies; real `WaveCleared(12)` checkpoint → screen shows → Continue → resumes (player profile snapshotted/restored during the test). **Do not disable those two roots — ever.**

- **18 Jul (latest)** — `UiSpriteSheetFlipbook` v2 — reusable animation-sheet controller: `[ExecuteAlways]` + frame-0 uv baked into the prefabs so the raw 5×5 sheet can never render (the user had seen the whole grid in edit mode); inspector options `Swap Speed` (fps, FormerlySerializedAs migration), `Loop` (off = one-shot, holds last frame), `Play On Enable`; public `Play()/Stop()/SetSheet()/IsPlaying`. King flipbooks set to 15 fps/loop. Verified both modes live (loop wraps; one-shot stops on the exact last cell); console clean.

- **18 Jul (latest)** — Checkpoint + MatchEnd screens animated: new reusable `UiEntranceAnimator` (staggered ease-out-back pop-in + fade of each active child, unscaled time, alloc-free, restores on disable) on both `Panel`s, and `UiSpriteSheetFlipbook` (RawImage uvRect grid flipbook — no slicing/Animator, unscaled, half-texel inset, auto aspect via fitter) playing the new king reaction sheets from `UI/AnimationSpriteSheets` on loop: thumka (happy) on Checkpoint + victory, fail (sad) on defeat — `MatchEndView` swaps sheets per outcome. Static king Images disabled (kept as fallback). Live-verified through the real defeat flow + checkpoint panel; console clean.

- **18 Jul (latest)** — Lightning strikes stamp a small ground crack: new public `V4CardVfx.StampCrack(point, radius, duration)` reuses the Earthquake's procedural crack decal (same texture/open/seal animation) with a `Quiet` flag that skips the quake's rumble/dust/pebble loop; `LightningStormView.Strike` stamps one under each struck victim (radius 0.8 → 1.6-unit decal, ~1.4 s incl. seal; knobs serialized on LightningStormView). Live-verified: 2 bolts → 2 cracks at the victims' feet, sealed + pooled; console clean.

- **18 Jul (latest)** — Lightning joins the SkyPoint: `LightningStormView` bolt origins now read `GameConfig.skyPoint` via its GameContext ref (viewport top-edge projection kept only as a no-context fallback; retires the 17-Jul screen-edge ruling). All three sky spells (meteor / arrows / lightning) now enter from the one shared point. Live-verified: real cast on a factory-spawned goblin, bolt From = (1.19, 36.40, 3.87) exactly; console clean.

- **18 Jul (latest)** — Shared SkyPoint (user ruling): every skyfall spell now launches from ONE fixed sky origin — new `GameConfigSO.skyPoint` = (1.19, 36.4, 3.87) in `GameConfig.asset`; `SkyfallEffect` takes an optional GameConfigSO ref (assigned on `FX_Meteor` + `FX_ArrowRain` prefabs, no scene edits) and starts every item there, falling back to the old dropHeight/lateralOffset when unset. Any future sky-entering object should read the same field. Lightning unchanged (top-screen-edge origin is a prior user ruling); Log keeps its 4-unit placement drop. Live-verified in Game scene: meteor + all 15 arrows spawn at the SkyPoint and fan to their landings; console clean.

- **18 Jul (latest)** — Fireball meteor "sudden turn" fixed: the angled drop (`FX_Meteor` SkyfallEffect `_lateralOffset` −0.8,−0.4) + rising world-space trail smoke made the trail kink into an elbow just before landing (aged smoke drifts vertical, fresh fire streak hugs the angled path; u² ease concentrates the divergence at the end). Data-only fix: lateral offset → (0,0), dead-vertical drop = one coherent column. Verified live in the Game scene (frame-stepped cast, staged captures); orange blast at landing confirms the tint fix in the real game; console clean.

- **18 Jul (latest)** — Blast prefab is USER-OWNED, runtime tint stomp fixed: user discarded all edits to `RealisticHeavyBombBlast` (prefab now hands-off) but its orange fire kept turning WHITE at runtime — `VfxSpawner` was overwriting the root `startColor` with `impactTint` (white on all cannon/mortar assets) on every impact. Fixed in `VfxSpawner` only: white tint = "as authored" (skipped), non-white tints still recolor (MageBolt purple kept), and the authored color is cached per pooled instance + restored on untinted spawns so tints can't leak across reuses. Verified: real cannonball impact leaves the blast orange (1, 0.31, 0), DPS lands, console clean.

- **18 Jul (later)** — Blast consolidation v2 (user ruling, supersedes the variants entry below): the user's hand-tuned RAW `RealisticHeavyBombBlast.prefab` (ThirdParty/EpicToonFX) is now THE single blast prefab for mortar + both cannons + fireball (`Projectile_MortarShell/_CannonBall/_TowerCannon.impactVfx` + `SpellVfxDirector._fireBlast` in both scenes). Fixed why its fire was invisible ("only black smoke"): legacy built-in shaders → rebound to URP mats (FireOrb/Mat_SmokeSoft/Mat_ParticleAdd, same as VFX_MortarBlast), root 8×8 flipbook module disabled (single-sprite texture now), fire alpha envelope 0→1→0 fixed to 1→1→0, Hierarchy scaling everywhere (fireball's runtime radius scale works). User's own tuning preserved. ⚠️ Blasts are silent now (prefab has no AudioSource). `VFX_FireballBlastBig`/`VFX_CannonBlast` unused, kept on disk. Live-verified (fire renders, cast→blast→DPS 182, pooled); console clean.

- **18 Jul** — Bomb-based blast family (user direction): every explosion now derives from Epic Toon FX `RealisticHeavyBombBlast` via prefab VARIANTS. NEW `VFX_FireballBlastBig` (exaggerated: ×1.35 baked + ×1.8 embers, ≈2× on screen with the runtime radius scale) is the shipped Fireball blast in Game + TestRange (`_fireShake` 0.5→0.6); NEW `VFX_CannonBlast` (×0.5 compact, silent — muzzle already booms) on `Projectile_CannonBall` + `Projectile_TowerCannon` (replacing the tiny `VFX_Impact`); mortar already bomb-based, untouched. Core fix: children flipped to Hierarchy particle scaling — `VfxSpawner`'s scale param previously only grew the root flash. Live-verified (cast → meteor → big blast → pooled reuse, DPS lands, peak-frame captures); console clean. See 11_MODULE_JUICE + 12_ASSET_STATUS.

- **18 Jul** — Spells cast ANYWHERE (user ruling, supersedes 17-Jul "must connect with an enemy"): Fireball/Lightning/Earthquake/Arrows/Freeze/Log are placeable at any point inside the map circle — empty ground included, player's choice. `PlacementValidator.IsValidSpellSpot` = map-circle check only; `SpellEffectSO.HasTargets` + Log override deleted (dead code). Verified with an empty target registry: all 6 cards valid on bare ground, still invalid outside `mapRadius`. See 13_PROGRESSION_V4 §5.6.

- **18 Jul** — TestRange is now a full prefab showcase + test rig: `PrefabShowcase` root with 90 prefab-linked instances in 6 labeled, spaced category fields (characters / buildings & towers / projectiles / VFX / third-party FX / UI on world-space canvases) + Arena far field, per-item name labels, items auto-seated via renderer bounds. New `ShowcaseEditOnly` hides the gallery in play mode (toggle on the PrefabShowcase root); `LightingRig_Test` (fill/rim/point lights, disabled by default) for lighting experiments. Runtime harness: `TestRangeContext` lists auto-filled (all 11 enemy defs, 6 spells, 23 VFX replay buttons, FX_Anchor assigned), `TestRangeUI` columns now drag-scrollable + Quake small/medium/BIG camera-shake buttons (`CameraShaker.AddTrauma`). Play-verified (showcase auto-hides, 17+39 buttons, console clean); scene saved.

- **18 Jul** — Log animation + lane preview (user pass): the Log now FALLS in from 4 units up (gravity ease-in, landing dust + small shake), rolls its exact `rollDistance`, then FADES out in place over 0.3 s (URP surface flipped transparent only for the fade, restored opaque on pooled reuse) instead of popping off; `Card_Log.fallDelaySeconds` = 0.35 so the damage sweep starts exactly at the visual landing. While DRAGGING the Log the ghost shows a straight ROLL-LANE strip (`GhostView.ShowLane` — rollDistance × width along rollDirection, near edge at the drop point, validity-tinted) instead of the radius circle; routed in `PlacementController.BeginDrag`, other cards unchanged. Live-verified per-step (fall y 4.41→land ≈0.35 s, roll exactly 9 units, alpha 1→0, pooled reuse opaque); console clean. See 13_PROGRESSION_V4 §5.4.

- **18 Jul** — Polish batch: camera-shake audit (tower-destroyed 0.8 / quake 0.3+rumbles / fireball 0.5 / lightning 0.5 already tuned; ADDED heavy-unit death thud 0.3 for Ogre-class + boss 0.45); cannonball trail rebuilt hot+visible + ember stream, bolt tracer brightened; cannon fire boom on a dedicated VFX_CannonMuzzle (shared flash kept silent for X-Bow); imported the tested `FrameRateSetter` (panel-max refresh → 120Hz where supported, tiered render scale) in both scenes, replacing the hardcoded 60; NEW `Splash.unity` (stretched bg + ROYAL DEFENSE title) with `SplashLoader` async-parallel Game load (activation after min-splash) — startup hitch hidden; build settings Splash(0)/Game(1). Play-verified end-to-end; console clean.

- **18 Jul** — CR-style card interaction layer (adapter over existing gameplay, all values in `Data/CardAnimConfig.asset`): finger-offset floating card at 1.12×; HUD↔battlefield handoff (card fades out / preview fades in at the hand panel's top edge; HUD release = clean cancel + landing squash); preview in-anim + valid pulse + invalid-enter shake + name label + commit confirm (freeze/pulse/fade); deployment ring brightens while dragging; energy bar spend TRAIL cells + regen pop + single cap glint + affordable-again card pulse; **deck cycle**: used slot holds empty ~0.84s then the Next-Up card flies in on an arc (~1.0s total), preview advances during the final 30%; pointer-ID ownership + focus-loss cancel. Merge/directional visuals N/A (no such mechanics). Play-verified all acceptance scenarios; gameplay authority untouched. New: `CardInteractionAnimationConfig`, `DeckCycleAnimator`.

- **18 Jul (final push, 6 commits)** — Mega feature/juice pass: mortar replaces the king's spells (king now static idle; MortarView yaw-tracks/recoils, shells lob from the tube with heavy-blast impact VFX from Epic Toon FX + shockwave/scorch/camera-shake; Fireball got the red variant); deploy ring auto-follows the tower + radius from `GameConfig.deploymentRadius` (DeployRingView); tower+king +10%; Arena is a prefab (`Arena_Stage1`); HUD header shows only Stage + progress bar; Level-Up/Tower-Invaded screens skinned to the LevelCompleteFail mocks (Revive = retry); floating damage numbers (pooled, kill-blow styling, drop shadows); Earthquake no longer vibrates enemies (DoT feedback aggregates ~0.9s + wind plumes); stylized black outlines on all units/tower/buildings (extra-material-slot inverted hull — mobile-cheap); FeedbackDirector wires SFX (Feel demo sounds + MagicArsenal impacts) + NiceVibrations haptics + shakes across combat/cards/flow; 60fps target + vSync off + particle budgets. Full QA run waves 1–8 (spells, knights, real checkpoint + unlock screen, L2 art swap): 0 errors/warnings. See AI_LOG for the full breakdown.

- **18 Jul** — Royal Tower level arts finalized: the 4 NEW tower models ARE the levels (`TowerLevelView._levelModels` = TowerLevel1..4; L4+L5 share the final art via clamp; old placeholder retired to `_hideOnInit`). Tower now VISIBLE in the edit scene (prefab saves TowerLevel1 active by default — it was only activated at runtime before). King auto-aligns to the ACTIVE tower's measured roof on every level change (`_kingRoot` + `AlignKing`; roofs are 3.09/2.52/2.28/2.38 — he was sunk 0.7 into the new L1 roof at the old fixed height); the upgrade surge glides him onto the new roof. All serialized in `RoyalTower_Full.prefab` (scene untouched). Live-verified all 5 levels + the animated surge. See 13_PROGRESSION_V4 §7.

- **18 Jul** — Fixed 7 console NREs from the HUD views (HandBarView / EnergyBarView / WaveBannerView / CheckpointScreenView / MatchEndView). The HUD is a prefab, so its serialized `_context` (GameContext) couldn't be baked — a prefab can't reference a scene object — and nothing wired it → null → NRE in each `Start`. Each view now falls back to `FindFirstObjectByType<GameContext>()` when `_context` is null (GameContext runs first at order −100). Live-verified: all views resolve, console 0 errors/warnings, HUD fully functional. This was the root of the "editor-session artifact" NREs from prior turns.

- **18 Jul** — Energy is now TIME-ONLY (user ruling): enemy kills no longer drop energy/orbs; the elixir bar fills solely from passive time regen. `EconomyConfigSO.killDropsEnergy` (= false) gates `OrbSpawner.OnEnemyKilled`; `EnergyBank` passive regen unchanged. Live-verified (3 kills add 0 energy, 0 orbs; +1 per 3.5 s regen). Flip `killDropsEnergy` on to restore the old kill-bounty hybrid. ⚠️ 3.5 s/elixir is now the only income — may want a faster rate. See 13_PROGRESSION_V4 §6.

- **18 Jul** — Deployment ring is now a DASHED white circle (matches the user mock). Rendered as 40 opaque white mesh dashes (`Art/Meshes/DeployRingDash.mesh` + `Art/Materials/Mat_DeployRingDash.mat`) on the `Arena/DeploymentRing` object with its LineRenderer disabled — URP wouldn't render a transparent/textured LineRenderer, so opaque mesh geometry was the reliable path. Scene diff is ring-only; play-verified. (Retune: regen the mesh — count/length/width.)

- **18 Jul** — Knight/spawn/material polish (user pass): (1) knights now steer AROUND the Royal Tower/buildings to reach a far-side target instead of grinding into it (`KnightUnit.SteerAroundStructures`, tangential avoidance, no navmesh); (2) wider + hash-jittered wave spawns so enemies arrive spread out and a little irregular (`SpawnPointProvider` spacing max(2.0, r·4) + bounded scatter; `WaveScheduler` per-unit spawn-time jitter — both zero-RNG); (3) fixed dull enemies — restored the darkened Goblin albedo to white and lifted 5 matte body materials to 0.3 smoothness (render-verified). All live-verified; compile clean; zero balance change. See 13_PROGRESSION_V4 §5.3/§5.5.

- **18 Jul** — Fixed knight "guard-ring trap" (user report): enemies attacking a knight from just OUTSIDE the tower/guard circle were never hit back — `KnightUnit` acquisition only accepted enemies inside the circle, but the knight is leashed to the ring edge so a standoff attacker just outside was un-targetable (its attack code has no circle restriction). Added `IsValidPrey` = inside circle OR within the knight's own melee reach; a knight now retaliates in place against any adjacent attacker while still never chasing distant out-of-circle enemies (leash untouched). Safe because knight footprint < attackRange. Live-verified (out-of-circle attacker took 150 dmg, was 0; distant enemy still ignored; knight stayed in ring). See 13_PROGRESSION_V4 §5.3.

- **18 Jul** — Log spell rework (two user deltas, same day). (1) It was practically unplaceable — Log is a rolling-lane spell but placement was gated on its tiny 0.8 drop-radius; introduced `SpellEffectSO.HasTargets` (virtual; default = "enemy inside `card.radius`", other 6 spells unchanged) delegated to by `PlacementValidator.IsValidSpellSpot`. (2) Final behavior per user: Log is placeable **anywhere** in the map (its `HasTargets` returns true) and ALWAYS rolls a **fixed forward direction** (world +Z = up the screen) from the drop point — was radial from center — up to `rollDistance` then vanishes, passing through buildings. `LogEffectSO.rollDirection` is the single source of truth driving both the sim (`Apply`) and the visual (`V4CardVfx`), so they can't drift. Zero balance numbers touched; compile clean; live-verified (fixed +Z from an off-center drop, valid on an empty field, in-lane hit / off-lane miss). See 13_PROGRESSION_V4 §5.4.

- **16 Jul (PM)** — Entire gameplay core implemented (35 scripts, 9 modules), all data assets authored from design tables, Game.unity wired, play-mode smoke test passed (spawning→combat→economy→placement→defeat all working, 0 errors). Spawn-point picks for waves where the GDD didn't specify are authored in `Waves_Level1.asset` and can be retuned freely. Note: enemy models currently slide (no AnimatorController yet); Mage uses Skeleton model; Boss reuses Ogre model.

- **16 Jul** — Project docs scaffolded from PDF brief + GDD v2 + meeting notes. Design deltas locked: 7 cards (Bomb Tower cut), no evolutions, strict range confirmed. JSONs in Documents/ marked outdated (v1).
